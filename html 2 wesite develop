<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Beautiful HTML — Starter</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
  <style>
    /* ===== Theme & tokens ===== */
    :root {
      --bg: 252 14% 10%;
      --panel: 0 0% 100% / 0.06;
      --text: 0 0% 100%;
      --muted: 0 0% 100% / 0.7;
      --brand: 265 88% 67%;
      --brand-2: 199 89% 60%;
      --ring: 0 0% 100% / 0.4;
      --radius: 1.25rem;
      --shadow: 0 20px 40px hsl(0 0% 0% / 0.35);
      --maxw: 1200px;
    }
    @media (prefers-color-scheme: light) {
      :root { --bg: 220 40% 97%; --panel: 0 0% 100% / 0.7; --text: 230 20% 12%; --muted: 230 10% 35%; --ring: 230 10% 20% / 0.2; --shadow: 0 10px 30px hsl(230 40% 20% / 0.08);}    }

    /* ===== Base ===== */
    * { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0; font: 16px/1.6 Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; color: hsl(var(--text));
      background: radial-gradient(1200px 500px at 20% -10%, hsl(var(--brand)/0.18), transparent 60%),
                  radial-gradient(1000px 400px at 110% 10%, hsl(var(--brand-2)/0.16), transparent 55%),
                  hsl(var(--bg));
      background-attachment: fixed;
    }
    .container { width: 92%; max-width: var(--maxw); margin-inline: auto; }
    .glass {
      background: linear-gradient(180deg, hsl(var(--panel)) 0%, hsl(var(--panel)) 100%);
      backdrop-filter: blur(14px) saturate(120%);
      border: 1px solid hsl(0 0% 100% / 0.08);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .btn {
      --_bg: linear-gradient(135deg, hsl(var(--brand)) 0%, hsl(var(--brand-2)) 100%);
      display: inline-flex; align-items: center; gap: .6rem; padding: .85rem 1.15rem; border-radius: 999px; border: 0;
      color: white; background: var(--_bg); text-decoration: none; font-weight: 700; letter-spacing: .2px;
      box-shadow: 0 10px 24px hsl(265 90% 60% / .25), inset 0 0 0 1px hsl(0 0% 100% / .12);
      transition: transform .15s ease, box-shadow .2s ease;
    }
    .btn:hover { transform: translateY(-1px); box-shadow: 0 14px 28px hsl(265 90% 60% / .35); }
    .btn.secondary { background: transparent; color: hsl(var(--text)); border: 1px solid hsl(0 0% 100% / .16); }
    
    /* ===== Navigation ===== */
    header { position: sticky; top: 0; z-index: 50; }
    .nav { display: flex; align-items: center; justify-content: space-between; padding: .9rem 0; }
    .brand { display:flex; align-items:center; gap:.65rem; font-weight:800; letter-spacing:.3px; }
    .logo { width: 36px; height: 36px; border-radius: 10px; background: linear-gradient(135deg, hsl(var(--brand)), hsl(var(--brand-2))); box-shadow: inset 0 0 20px hsl(0 0% 100% / .2), 0 8px 18px hsl(265 80% 60% / .35); }
    .nav a { color: hsl(var(--muted)); text-decoration: none; margin-left: 1.1rem; font-weight: 600; }
    .nav a:hover { color: hsl(var(--text)); }

    /* ===== Hero ===== */
    .hero { padding: 7rem 0 5rem; position: relative; overflow: clip; }
    .hero-grid { display: grid; grid-template-columns: 1.15fr 1fr; gap: 2rem; align-items: center; }
    .eyebrow { color: hsl(var(--muted)); text-transform: uppercase; letter-spacing: .25em; font-weight: 700; font-size: .8rem; }
    h1 { font-family: "Playfair Display", serif; font-size: clamp(2.5rem, 4vw + 1rem, 4rem); line-height: 1.05; margin: .6rem 0 1rem; }
    .lead { font-size: 1.1rem; color: hsl(var(--muted)); margin-bottom: 1.6rem; }
    .hero-card { position: relative; padding: 1.2rem; }
    .hero-illustration { aspect-ratio: 4 / 3; border-radius: calc(var(--radius) - .4rem); width: 100%; display: grid; place-items: center; overflow: hidden; }
    .orb { position: absolute; filter: blur(40px); opacity: .6; border-radius: 50%; }
    .orb.a { width: 220px; height: 220px; background: hsl(var(--brand)/.8); left: -40px; top: -40px; }
    .orb.b { width: 200px; height: 200px; background: hsl(var(--brand-2)/.85); right: -30px; bottom: -30px; }

    /* ===== Features ===== */
    .section { padding: 3.5rem 0; }
    .section h2 { font-size: clamp(1.6rem, 2.6vw + .8rem, 2.4rem); margin: 0 0 .75rem; }
    .kicker { color: hsl(var(--muted)); margin-bottom: 1.2rem; }
    .grid { display: grid; gap: 1.2rem; grid-template-columns: repeat(12, 1fr); }
    .col-4 { grid-column: span 4; }
    .col-6 { grid-column: span 6; }
    .col-12 { grid-column: span 12; }
    .card { padding: 1.2rem; }
    .icon { width: 42px; height: 42px; border-radius: 12px; display:grid;place-items:center; background: linear-gradient(135deg, hsl(var(--brand)/.9), hsl(var(--brand-2)/.9)); color: white; box-shadow: inset 0 0 0 1px hsl(0 0% 100% / .2); font-weight: 800; }

    /* ===== Showcase ===== */
    .masonry { columns: 3 280px; column-gap: 1rem; }
    .masonry img { width: 100%; border-radius: calc(var(--radius) - .5rem); display: block; margin-bottom: 1rem; filter: saturate(1.05) contrast(1.05); }

    /* ===== CTA & Footer ===== */
    .cta { text-align: center; padding: 4rem 1rem; }
    footer { padding: 2rem 0 3rem; color: hsl(var(--muted)); text-align: center; }

    /* ===== Utilities ===== */
    .row { display: flex; gap: .7rem; flex-wrap: wrap; }
    .tag { font-size: .8rem; padding: .45rem .7rem; border-radius: 999px; border: 1px dashed hsl(0 0% 100% / .2); color: hsl(var(--muted)); }

    /* ===== Responsive ===== */
    @media (max-width: 880px) {
      .hero { padding-top: 5rem; }
      .hero-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <!-- ===== Nav ===== -->
  <header class="glass">
    <div class="container nav">
      <a class="brand" href="#top" aria-label="Home">
        <div class="logo" aria-hidden="true"></div>
        <span>Beautiful <span style="opacity:.7">HTML</span></span>
      </a>
      <nav aria-label="Primary">
        <a href="#features">Features</a>
        <a href="#showcase">Showcase</a>
        <a href="#contact">Contact</a>
        <button class="btn secondary" id="themeToggle" aria-pressed="false" aria-label="Toggle theme">🌗</button>
      </nav>
    </div>
  </header>

  <!-- ===== Hero ===== -->
  <main id="top" class="hero">
    <div class="container hero-grid">
      <div>
        <p class="eyebrow">Made with pure HTML + CSS</p>
        <h1>Craft a gorgeous, modern landing page in minutes.</h1>
        <p class="lead">Clean typography, soft glass panels, and a playful gradient aura — all in one single file. Copy, remix, and launch your idea today.</p>
        <div class="row">
          <a class="btn" href="#features">Explore features</a>
          <a class="btn secondary" href="#showcase">See examples</a>
        </div>
        <div class="row" style="margin-top:1rem">
          <span class="tag">Responsive</span>
          <span class="tag">Glassmorphism</span>
          <span class="tag">Accessible</span>
          <span class="tag">No libraries</span>
        </div>
      </div>

      <div class="hero-card glass">
        <div class="hero-illustration glass">
          <svg viewBox="0 0 300 225" width="100%" height="100%" role="img" aria-label="Decorative abstract illustration">
            <defs>
              <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0%" stop-color="hsl(265 88% 67%)" />
                <stop offset="100%" stop-color="hsl(199 89% 60%)" />
              </linearGradient>
            </defs>
            <rect x="0" y="0" width="300" height="225" rx="18" fill="url(#g)" opacity=".2" />
            <g fill="none" stroke="url(#g)" stroke-width="2">
              <path d="M0 120 Q75 20 150 120 T300 120" />
              <path d="M0 150 Q75 50 150 150 T300 150" opacity=".6" />
              <path d="M0 90 Q75 -10 150 90 T300 90" opacity=".3" />
            </g>
            <g fill="white" opacity=".9">
              <circle cx="55" cy="60" r="6"/>
              <circle cx="245" cy="40" r="4"/>
              <circle cx="210" cy="190" r="5"/>
            </g>
          </svg>
        </div>
        <span class="orb a"></span>
        <span class="orb b"></span>
      </div>
    </div>
  </main>

  <!-- ===== Features ===== -->
  <section id="features" class="section">
    <div class="container">
      <h2>Delightful by default</h2>
      <p class="kicker">A small but mighty set of components to get you going.</p>
      <div class="grid">
        <article class="col-4 card glass">
          <div class="icon">A</div>
          <h3>Accessible</h3>
          <p>Semantic HTML, large hit targets, and high-contrast defaults for readability in any theme.</p>
        </article>
        <article class="col-4 card glass">
          <div class="icon">R</div>
          <h3>Responsive</h3>
          <p>Mobile-first layout with an adaptive grid, fluid type, and flexible media that just works.</p>
        </article>
        <article class="col-4 card glass">
          <div class="icon">F</div>
          <h3>Fast</h3>
          <p>Zero dependencies. One file. Delightfully quick to load and easy to drop into any project.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- ===== Showcase ===== -->
  <section id="showcase" class="section">
    <div class="container">
      <h2>Showcase</h2>
      <p class="kicker">Drop in your screenshots or photos. The masonry layout handles the rest.</p>
      <div class="masonry">
        <img src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?q=80&w=1200&auto=format&fit=crop" alt="Aurora over mountains">
        <img src="https://images.unsplash.com/photo-1580674285053-1cac5b04433f?q=80&w=1200&auto=format&fit=crop" alt="Minimal desk setup">
        <img src="https://images.unsplash.com/photo-1529101091764-c3526daf38fe?q=80&w=1200&auto=format&fit=crop" alt="Tropical leaf close-up">
        <img src="https://images.unsplash.com/photo-1500534314209-a25ddb2bd429?q=80&w=1200&auto=format&fit=crop" alt="Starry night sky">
        <img src="https://images.unsplash.com/photo-1519681393784-d120267933ba?q=80&w=1200&auto=format&fit=crop" alt="Code on screen">
        <img src="https://images.unsplash.com/photo-1614851099511-773084f6911d?q=80&w=1200&auto=format&fit=crop" alt="Abstract colorful shapes">
      </div>
    </div>
  </section>

  <!-- ===== Call to Action ===== -->
  <section id="contact" class="section cta">
    <div class="container glass">
      <h2>Ready to build something beautiful?</h2>
      <p class="kicker">Use this as your starting point — then make it yours.</p>
      <div class="row" style="justify-content:center; margin-top:.5rem; padding-bottom: 1.2rem;">
        <a class="btn" href="#top">Get started</a>
        <a class="btn secondary" href="#" onclick="share()">Share</a>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <small>© <span id="year"></span> Beautiful HTML Starter. Crafted with ♥︎.</small>
    </div>
  </footer>

  <script>
    // Theme toggle: switches a data-theme attr you can style if desired
    const btn = document.getElementById('themeToggle');
    btn?.addEventListener('click', () => {
      const pressed = btn.getAttribute('aria-pressed') === 'true';
      btn.setAttribute('aria-pressed', String(!pressed));
      document.documentElement.toggleAttribute('data-light');
      // quick swap for demo: invert brand hues when light
      document.body.style.transition = 'background .35s ease';
    });

    // Year in footer
    document.getElementById('year').textContent = new Date().getFullYear();

    // Share API helper (falls back to copy)
    async function share() {
      const url = location.href;
      try {
        if (navigator.share) { await navigator.share({ title: document.title, url }); }
        else { await navigator.clipboard.writeText(url); alert('Link copied to clipboard!'); }
      } catch (e) { console.error(e); }
    }
  </script>
</body>
</html>
